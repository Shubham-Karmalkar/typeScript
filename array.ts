const superHero: string[] = [];
const heroPower: number[] = [];

const newPower: Array<number> = [];

type User = {
    name: string,
    isActive: boolean
}

const allUsers: User[] = [];

allUsers.push({ name: "shubham", isActive: true })

const MLModels: number[][] = [
    [1, 234, 54353]
]

superHero.push("spiderman")
heroPower.push(23)

export { }