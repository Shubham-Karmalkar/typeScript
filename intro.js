/**
 * 1. ultimately typescript is converted to javascript
 * 2. its very much like a developer tool
 * 3. it checks the type and prevent the some of the bugs before run time
 * 4. typescript is the superset of javascript that means its has all  the functionalities of javascript
 *      and also some new features like type are added in type script.
 */
var user = {
    name: "shubham",
    age: 25
};
console.log('shubham');
console.log(user.name);
console.log(user.email); //see its is showing the error as email property does not exist in user
