command: 
    tsc --init // to create TypeScript config file for production usage

* we write comlete typescipt code in src file and then we compile and with the help of
    tsc config file we give path of folder dist(distribution) to save end javascript compiled files

* enter the command "tsc -w" that is watch mode which will created watch our typescript file and refere the 
    config file of tsc before doing any changes